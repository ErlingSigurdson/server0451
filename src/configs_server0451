// Текущее предписываемое положение нагрузки для данного экземпляра программы.
CURRENT_CMD=0
// Команда для установки предписываемого положения нагрузки "ВКЛ".
ASCII_CMD_ON=AT+SETLOAD=ON
// Команда для установки предписываемого положения нагрузки "ВКЛ", помещаемая в URI GET-запроса.
URI_CMD_ON=/setloadon
// Команда для установки предписываемого положения нагрузки "ВКЛ", помещаемая в тело POST-запроса (в пару параметр+значение).
VAL_CMD_ON=cmd=setloadon
// Команда для установки предписываемого положения нагрузки "ВЫКЛ".
ASCII_CMD_OFF=AT+SETLOAD=OFF
// Команда для установки предписываемого положения нагрузки "ВЫКЛ", помещаемая в URI GET-запроса.
URI_CMD_OFF=/setloadoff
// Команда для установки предписываемого положения нагрузки "ВЫКЛ", помещаемая в тело POST-запроса (в пару параметр+значение).
VAL_CMD_OFF=cmd=setloadoff
// Команда для смены предписываемого положения нагрузки на противоположное.
ASCII_CMD_TOGGLE=AT+SETLOAD=TOGGLE
// Команда для смены предписываемого положения нагрузки на противоположное, помещаемая в URI GET-запроса.
URI_CMD_TOGGLE=/setloadtoggle
// Команда для смены предписываемого положения нагрузки на противоположное, помещаемая в тело POST-запроса (в пару параметр+значение).
VAL_CMD_TOGGLE=cmd=setloadtoggle
// Запрос текущего предписываемого положения нагрузки.
REQUEST_CMD=GETLOAD
